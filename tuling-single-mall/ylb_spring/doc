ylb_spring_01的目标是实现一个bean容器对bean进行定义、注册、和获取的雏形结构，

ylb_spring_02的目标是
    （1）Bean 的创建交给容器，而不是我们在调用时候传递一个实例化好的 Bean 对象
    （2）考虑单例对象，在对象的二次获取时是可以从内存中获取对象的
    （3）完善基础容器框架的类结构体，否则将来就很难扩容进去其他的功能了

    技术点：
        BeanFactory 定义一个获取bean的接口
        AbstractBeanFactory 对BeanFactory的获取bean的接口进行实现，在实现时使用了模板方法，定义了新的抽象方法createBean和getBeanDefinition
        AbstractAutowireCapableBeanFactory 实现了createBean方法
        DefaultListableBeanFactory 实现了getBeanDefinition方法
        SingletonBeanRegistry和DefaultSingletonBeanRegistry定义了单例bean的存储和获取
    不足处：
        我们扩充了 Bean 容器的功能，把实例化对象交给容器来统一处理，但在我们实例化对象的代码里并没有考虑对象类是否含构造函数，也就是说如果我们去实例化一个含有构造函数的对象那么就要抛异常了

ylb_spring_03的目标是解决有参构造函数的实例化

    不足之处：
        在创建对象实例化这我们还缺少什么？其实还缺少一个关于类中是否有属性的问题，如果有类中包含属性那么在实例化的时候就需要把属性信息填充上，这样才是一个完整的对象创建

ylb_spring_04的目标是有类中包含属性那么在实例化的时候就需要把属性信息填充上.

    不足之处：
        (1)本章节我们暂时不会考虑 Bean 的循环依赖
        (2)Bean对象的定义、注册和属性填充，以及最终获取对象调用方法都是通过手动实现的


ylb_spring_05的目标是Bean对象的定义、注册和属性填充，以及最终获取对象调用方法通过配置文件自动实现

    本章核心内容：
        （1）资源加载器  Resource  ResourceLoader
        （2）xml资源处理类
        （3）使用资源，将配置信息注册到spring容器中去
