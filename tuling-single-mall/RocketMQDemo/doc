1,RocketMQTemplate的功能：
    （1）普通消息：无返回值，只负责发送消息⽽不等待服务器回应且没有回调函数触发
    （2）同步消息：有返回值SendResult，等到消息发送成功后才算结束
    （3）异步消息：无返回值，需要传入回调类。无需等待消息是否发送成功。

    （4）顺序发送：一个Topic下可能会有多个消息队列queue，在发送消息时可以指定要发送到哪个消息队列中。

2，RocketMQListener<T>的功能：
    （1）该接口监听某个消费者组和topic中的消息。
    （2）消费模式：集群模式和广播模式

